
use std::collections::{HashMap, LinkedList};

#[derive(Debug)]
pub struct Trie{
    i32: value,
    pub children: HashMap<i32, TrieNode>,
}
pub struct TrieNode{
    
}

#[cfg(test)]
mod tests {
    use super::Trie;
    #[test]
    pub fn test1() {

    }
}